# Partition table for RIVR Node (ESP32, 4MB flash, single-app / no OTA)
# App partition: ~2MB – large enough for a debug build of ESP-IDF 5.x + Rust
#
# IDF v5.5 bootloader for ESP32 can exceed 28 KB, so the partition table is
# placed at 0x9000 (instead of the classic 0x8000) giving 32 KB to the
# bootloader.  Controlled by CONFIG_PARTITION_TABLE_OFFSET in sdkconfig.defaults.
#
# Layout:
#   0x1000 – 0x9000  : bootloader (max 32 KB)
#   0x9000 – 0xA000  : partition table (4 KB, written by esptool at 0x9000)
#   0xA000 – 0x10000 : nvs (24 KB) + phy_init (4 KB) + padding
#   0x10000 – 0x200000 : factory app (~2MB - 64KB = 1984 KB)
#
# Name,   Type, SubType,  Offset,   Size,   Flags
nvs,      data, nvs,      0xA000,   0x6000,
phy_init, data, phy,      0x10000,  0x1000,
factory,  app,  factory,  0x20000,  0x1E0000,
